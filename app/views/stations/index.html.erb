<div class="container">
<h1>Stations:</h1>

<% Station.all.each do |station| %>
<p><%= link_to "#{station.name}", station_path(station) %></p>
<%end %>

<p><%= link_to "New Station", new_station_path, :class=>"btn btn-primary" %></p>
</div>
